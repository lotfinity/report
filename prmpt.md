You are converting a real-estate agency website into a reusable HTML report template.

Your job is not to copy the original website content.
Your job is to understand the structure, section roles, layout patterns, and content slots.

Given the extracted HTML/DOM summary below:

[PASTE STRUCTURE]

Return a template blueprint with:
1. page purpose
2. visual style summary
3. ordered sections
4. detected section type
5. reusable content slots
6. what kind of new report content should go into each slot
7. recommended max text length per slot
8. whether the section should be kept, removed, or repurposed

Output valid JSON only.